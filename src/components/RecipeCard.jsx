function RecipeCard({ recipe, favorite, toggleFavorite, selectRecipe }) { return <article className="recipe-card" onClick={() => selectRecipe(recipe)}><div className={`recipe-art ${recipe.accent}`}><span>{recipe.emoji}</span><button className={favorite ? "save saved" : "save"} onClick={(event) => { event.stopPropagation(); toggleFavorite(recipe.id); }}>{favorite ? "♥" : "♡"}</button>{recipe.match > 0 && <small>{Math.round(recipe.match / recipe.ingredients.length * 100)}% pantry match</small>}</div><div className="recipe-copy"><div className="recipe-meta"><span>{recipe.category}</span><b>★ {recipe.rating}</b></div><h3>{recipe.name}</h3><p><span>◷ {recipe.time} min</span><span>◉ {recipe.calories} kcal</span></p></div></article>; }
export default RecipeCard;
