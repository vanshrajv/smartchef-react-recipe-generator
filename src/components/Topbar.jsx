function Topbar({ darkMode, setDarkMode, query, setQuery, setPage, currentTime }) { const date = currentTime.toLocaleDateString(undefined, { weekday: "short", day: "2-digit", month: "short", year: "numeric" }).toUpperCase(); const time = currentTime.toLocaleTimeString(undefined, { hour: "2-digit", minute: "2-digit", second: "2-digit" }); return <header className="topbar"><div className="mobile-brand"><span>SC</span> smartchef</div><label className="global-search"><span>⌕</span><input value={query} onChange={(event) => setQuery(event.target.value)} onFocus={() => setPage("discover")} placeholder="Search recipes, ingredients..." /><kbd>⌘ K</kbd></label><div className="top-actions"><button className="icon-button" title="Notifications">♧<i /></button><button className="theme-toggle" onClick={() => setDarkMode(!darkMode)}>{darkMode ? "☼" : "☾"}</button><div className="date-chip"><span>{date}</span><strong>{time}</strong></div></div></header>; }
export default Topbar;
