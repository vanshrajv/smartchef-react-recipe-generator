import RecipeCard from "../components/RecipeCard";
import SectionHeading from "../components/SectionHeading";
function Discover({ recipes, query, setQuery, favorites, toggleFavorite, selectRecipe }) { return <div className="page"><SectionHeading eyebrow="THE RECIPE INDEX" title="Discover your next favourite" /><div className="discover-tools"><label><span>⌕</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search by recipe or mood" /></label><div className="filter-chips"><button className="selected">All recipes</button><button>Under 30 min</button><button>Vegetarian</button><button>High protein</button></div></div><p className="result-count">{recipes.length} recipes that fit your kitchen right now</p><div className="recipe-grid discover-grid">{recipes.map((recipe) => <RecipeCard key={recipe.id} recipe={recipe} favorite={favorites.includes(recipe.id)} toggleFavorite={toggleFavorite} selectRecipe={selectRecipe} />)}</div></div>; }
export default Discover;
