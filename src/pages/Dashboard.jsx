import RecipeCard from "../components/RecipeCard";
import SectionHeading from "../components/SectionHeading";
function Dashboard({ pantry, recipes, favorites, mealPlan, groceries, selectRecipe, toggleFavorite, setPage, flash, greeting, currentTime }) { const planned = Object.values(mealPlan).filter(Boolean).length; const date = currentTime.toLocaleDateString(undefined, { weekday: "long", day: "numeric", month: "long", year: "numeric" }).toUpperCase(); return <div className="page dashboard"><div className="welcome"><div><span className="eyebrow">{date} · {currentTime.toLocaleTimeString(undefined, { second: "2-digit" })}</span><h1>{greeting}, Vansh<span>.</span></h1><p>Let's make something worth remembering.</p></div><button className="primary-button" onClick={() => { setPage("discover"); flash("Showing your best pantry matches"); }}>Find a recipe <span>→</span></button></div><section className="hero-panel"><div className="hero-text"><span className="hero-kicker">TODAY'S EDITORIAL PICK</span><h2>Comfort food,<br /><i>without the wait.</i></h2><p>Silky miso noodles with greens and a little chilli crunch. Your 15-minute answer to a long day.</p><button className="light-button" onClick={() => selectRecipe(recipes[0])}>Cook this tonight <span>↗</span></button></div><div className="hero-dish">🍜<span>15<br /><small>MIN</small></span></div><div className="hero-note">NO. 014 / QUICK COMFORT</div></section><div className="metric-row"><div><span>◈</span><strong>{pantry.length}</strong><small>items in pantry</small></div><div><span>▦</span><strong>{planned}<em>/ 7</em></strong><small>meals planned</small></div><div><span>□</span><strong>{groceries.length}</strong><small>on grocery list</small></div><div><span>♥</span><strong>{favorites.length}</strong><small>saved recipes</small></div></div><section className="content-section"><SectionHeading eyebrow="BASED ON YOUR PANTRY" title="You could make this" action="See all recipes" onAction={() => setPage("discover")} /><div className="recipe-grid">{recipes.slice(0, 3).map((recipe) => <RecipeCard key={recipe.id} recipe={recipe} favorite={favorites.includes(recipe.id)} toggleFavorite={toggleFavorite} selectRecipe={selectRecipe} />)}</div></section></div>; }
export default Dashboard;
