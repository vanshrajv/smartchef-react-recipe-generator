import SectionHeading from "../components/SectionHeading";
function Planner({ recipes, days, mealPlan, setMealPlan }) { return <div className="page"><SectionHeading eyebrow="THE WEEK AHEAD" title="Your meal plan" action="Clear week" onAction={() => setMealPlan({})} /><div className="planner-intro"><span>WEEK 42</span><p>Three meals planned. Four delicious possibilities still open.</p></div><div className="planner-grid">{days.map((day) => { const meal = recipes.find((recipe) => recipe.id === mealPlan[day]); return <article className={meal ? "day-slot filled" : "day-slot"} key={day}><div className="day-name"><b>{day.slice(0, 3).toUpperCase()}</b><span>{day}</span></div>{meal ? <div className={`planned-meal ${meal.accent}`}><span>{meal.emoji}</span><strong>{meal.name}</strong><small>{meal.time} min · {meal.calories} kcal</small><button onClick={() => setMealPlan({ ...mealPlan, [day]: undefined })}>Remove</button></div> : <div className="empty-slot"><span>+</span><p>Choose a meal</p></div>}<select value={mealPlan[day] || ""} onChange={(event) => setMealPlan({ ...mealPlan, [day]: Number(event.target.value) || undefined })}><option value="">Add meal...</option>{recipes.map((recipe) => <option value={recipe.id} key={recipe.id}>{recipe.name}</option>)}</select></article>; })}</div></div>; }
export default Planner;
