import SectionHeading from "../components/SectionHeading";
function Pantry({ items, allItems, setItems }) { const toggle = (item) => setItems(items.includes(item) ? items.filter((entry) => entry !== item) : [...items, item]); return <div className="page"><SectionHeading eyebrow="INVENTORY / LIVE" title="My pantry" action={`${items.length} ingredients`} /><div className="pantry-hero"><div><span className="eyebrow">SMART MATCHING ACTIVE</span><h2>Your kitchen, <i>at a glance.</i></h2><p>Tap ingredients to tell SmartChef what you have on hand. We'll handle the rest.</p></div><div className="pantry-orbit">{items.slice(0, 3).map((item) => <span key={item}>{item.slice(0, 2)}</span>)}</div></div><div className="pantry-list">{allItems.map((item) => <button key={item} className={items.includes(item) ? "pantry-item active" : "pantry-item"} onClick={() => toggle(item)}><span>{item.slice(0, 2).toUpperCase()}</span><strong>{item}</strong><b>{items.includes(item) ? "✓" : "+"}</b></button>)}</div></div>; }
export default Pantry;
