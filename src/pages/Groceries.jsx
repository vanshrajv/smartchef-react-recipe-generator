import { useState } from "react";
import SectionHeading from "../components/SectionHeading";
function Groceries({ items, addItems, removeItem }) { const [newItem, setNewItem] = useState(""); const add = () => { if (newItem.trim()) { addItems([newItem.trim()]); setNewItem(""); } }; return <div className="page"><SectionHeading eyebrow="THE NEXT SHOP" title="Grocery list" action="Print list" onAction={() => window.print()} /><div className="grocery-layout"><section className="grocery-card"><div className="grocery-card-head"><span>TO BUY</span><strong>{items.length} items</strong></div><div className="grocery-add"><input value={newItem} onChange={(event) => setNewItem(event.target.value)} onKeyDown={(event) => event.key === "Enter" && add()} placeholder="Add an item..." /><button onClick={add}>Add</button></div>{items.map((item) => <label className="grocery-item" key={item}><input type="checkbox" onChange={() => removeItem(item)} /><span>{item}</span><small>Produce</small></label>)}</section><aside className="grocery-tip"><span>✦</span><h3>One small shortcut</h3><p>Group your shop by aisle and you'll spend less time circling the store.</p><button onClick={() => addItems(["Olive oil"])}>+ Add pantry staple</button></aside></div></div>; }
export default Groceries;
